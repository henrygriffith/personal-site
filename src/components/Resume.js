import React from "react";

const Resume = () => {
  return (
    <div id="resume-container">
      <button className="content-button resume-download">Download</button>
    </div>
  );
};

export default Resume;
